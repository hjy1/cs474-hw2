(declare-const p Bool)
(declare-const q Bool)
(declare-const r Bool)
(define-fun formula () Bool
    (and (or q (not r)) (or (not p) r) (or (not p) r p) (or p q (not q)) (or (not r) q))
)
(assert formula)

(check-sat)
(get-model)